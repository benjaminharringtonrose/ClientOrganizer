import { StyleSheet } from "react-native";
import { Color } from "./Color";

export const TextStyles = StyleSheet.create({
  header: {
    fontSize: 20,
    fontWeight: "500",
    color: Color.white,
  },
});
